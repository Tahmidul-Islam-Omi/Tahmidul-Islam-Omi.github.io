# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# Raw image sources kept in the repo root (only used locally to produce the
# optimized copies in /public, e.g. public/projects/*.webp and profile.webp).
# The images the site actually serves live under /public and ARE committed.
/*.png
/*.jpg
/*.jpeg
/*.webp

# raw resume source in repo root (the served copy lives at public/resume.pdf)
/Tahmidul Islam Omi (Resume).pdf

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
